# --- Secrets & host-specific inputs (NEVER commit; public repo) ---
project.md
environment.txt
deploy.local.env
.env
.env.*
!.env.example

# --- Node / build ---
node_modules/
dist/
build/
*.tsbuildinfo
coverage/

# --- App runtime data (config volume contents) ---
*.sqlite
*.sqlite-journal
*.db
*.db-journal
dev.db*
/data/
/logs/
/thumbnails/

# --- Misc ---
.DS_Store
*.log

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Measurement report — contains embedded photographs of the site and of people.
# Kept locally and as a private artifact; never published to the public repository.
docs/reports/
